<Window x:Class="MOTOSalon.Главная"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MOTOSalon"
        mc:Ignorable="d"
        Title="Главная" MinHeight="400" MinWidth="623.5" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight" Loaded="Window_Loaded" Height="442" Width="703.5">
    <Grid Margin="0,0,610,333">
        <Menu x:Name="menu" HorizontalAlignment="Left" Height="36" Margin="0,0,-609,0" VerticalAlignment="Top" Width="695">
            <MenuItem Header=""/>
            <MenuItem Header=""/>
            <MenuItem Header="Файл" Height="36" Width="52"/>
            <MenuItem Header="Правка"/>
            <MenuItem Header="Товар на аскладе " Click="MenuItem_Click"/>
            <MenuItem Header="MenuItem"/>
            <MenuItem Header="MenuItem"/>
            <MenuItem Header=""/>
            <MenuItem Header="О программе"/>
        </Menu>
    </Grid>
</Window>
